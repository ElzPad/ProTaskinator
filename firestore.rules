rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /tasks/{task_id} {
      allow create, read, write, update, delete: if request.auth.uid == resource.data.createdBy.uid;
    }
  }
}